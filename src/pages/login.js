import React from 'react';
import Button from '@mui/material/Button';

const Login = () => {
    return (
        <div>
           <h1>Login</h1>
        </div>
    );
};

export default Login;
